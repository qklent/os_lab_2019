TCP is like the rules for packaging (envelope, box, tracking number, etc.)
IP is like the addressing system (street address, city, country)


socket components:
    Components:

    IP Address (like a street address)
    Port Number (like an apartment number)
    Protocol (TCP or UDP)
